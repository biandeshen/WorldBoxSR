# v0.5 backlog — World Stories

Release gate: #208. Version promise: **this world has a history I can follow, revisit and remember.**

v0.5 is a presentation/query stage over bounded authoritative history. It does not add a second source of world truth.

## Ordered slices

### 1. Causal Event Card — #209 / #211 — DONE
- [x] extend retained-reference resolution for current polity/warband entities already used by authoritative events;
- [x] pure Event Card projection from one retained event + resolved subject/causes;
- [x] readable provenance: headline/detail, year, event id/type;
- [x] explicit resolved/unresolved Subject and Causes rows;
- [x] retained event refs can open another Event Card;
- [x] current map-capable refs can center/navigate to their world object or polity capital;
- [x] command/expired refs remain visibly unresolved;
- [x] deterministic tests prove projection/navigation are snapshot + RNG neutral;
- [x] real Chromium creates a fresh causal succession through real Lightning input, opens its Event Card, follows the retained death-event cause, follows a current map reference, and verifies the serialized authoritative world fingerprint is unchanged;
- [x] manual visual review confirms the succession card, unavailable dead-ruler subject, retained Lightning/death causal link, and map-navigation card are player-readable;
- [x] first-slice scope does not change Chronicle representative-event selection policy.

### 2. Focused story trail — #212 / #213 — DONE
- [x] selected retained event or supported stable entity ref can become presentation-only focus;
- [x] exact history queries cover human, creature, settlement, polity, warband and one-hop event focus without current-state association guessing;
- [x] focused trail is chronological oldest→newest with fixed limit 8;
- [x] event focus includes the selected retained event plus direct retained children only, never recursive causes-of-causes;
- [x] unavailable focused entities remain identifiable and can still expose retained explicit history;
- [x] Event Card exposes `Follow this event` and supported reference-row `Follow story` actions;
- [x] trail rows reopen the existing Event Card while focus remains active; Clear removes only presentation focus;
- [x] resolved focus may reuse current map navigation; reset clears focus;
- [x] deterministic tests prove exact membership/order/limit and snapshot + RNG neutrality;
- [x] natural seed45 Chromium follows `Eldergate Realm` (polity #1) into explicit retained events `[21,22,23,24,27,28,29,32]`, opens event #22, clears focus, and verifies authoritative world fingerprint is unchanged;
- [x] manual screenshot review confirms the compact 8-event trail remains readable inside Chronicle rather than becoming an analytics dashboard.

### 3. Bookmarks / watchlist — #214 / #215 — DONE
- [x] explicit Pin/Unpin for retained events and supported human/creature/settlement/polity/warband refs;
- [x] Watchlist is presentation-only, capped at 6 stable refs, outside world snapshots/history/save authority;
- [x] same-tab `sessionStorage` persistence with corrupt, duplicate, unsupported and over-cap entries sanitized safely;
- [x] current resolution is recomputed when rendered; removed entities and evicted events remain pinned but truthfully unavailable;
- [x] Event Card selected event and supported Subject/Cause rows expose Pin/Unpin independently from Follow/Open/Map;
- [x] compact Watchlist reuses existing Open/Map/Follow paths plus Unpin/Clear all;
- [x] reset preserves the stable session refs and re-resolves them against the new ready world;
- [x] rehydration waits for authoritative showcase warmup (`scene.booting === false`) before resolving refs, fixing the real reload race caught by Chromium;
- [x] deterministic tests prove cap/toggle/storage sanitization/current re-resolution/unavailable refs and snapshot + RNG neutrality;
- [x] real Chromium pins `event:163` + `polity:1`, browses away and returns, same-tab reload restores both refs, then Unpin/Clear leaves session storage empty;
- [x] Watchlist actions preserve authoritative world fingerprints before reload and after reload;
- [x] manual visual review confirms both the pre-reload and rehydrated `2/6 pinned` Watchlist stay compact and subordinate to the world view.

### 4. Chronicle navigation/readability — #216 / #217 — DONE
- [x] Chronicle adds exactly four compact game-facing lenses: `Highlights · Recent · Conflict · Rule`;
- [x] `Highlights` remains byte-for-byte equivalent at the projection boundary to the existing `civilizationChronicle(world, { limit: 7 })` representative selection policy;
- [x] `Recent` is newest-first retained readable World Story events only, with no scoring or inferred relevance;
- [x] `Conflict` contains only explicit war/peace/warband/conquest/rebellion story types;
- [x] `Rule` contains only explicit polity founding/dissolution/ruler-transition story types;
- [x] every lens is capped at 7 rows and empty lenses stay truthfully empty instead of backfilling unrelated history;
- [x] rows retain `data-event-id` and reopen the same Event Card path; the current Event Card remains usable while lenses switch;
- [x] lens state is presentation-only and does not enter session/local storage, snapshots, history or simulation state;
- [x] legacy history controls remain intact in the legacy renderer; ignored World/Selection/order controls are hidden only in Phaser mode;
- [x] pure/runtime tests lock default equivalence, exact memberships/order/limit, empty state and snapshot/RNG neutrality;
- [x] real Chromium on seed45 records Highlights `[163,135,134,120,119,117,115]`, real-clicks Recent `[163,162,159,158,157,156,155]`, Conflict `[134,120,119,117,115,114,111]`, Rule `[163,162,159,158,157,156,155]`, opens non-default Event #163, then restores the exact original Highlights IDs;
- [x] the same browser gate establishes a Focused Story plus a Watchlist pin before switching lenses and proves both presentation states remain unchanged throughout;
- [x] the serialized authoritative world fingerprint and paused state remain unchanged throughout lens navigation;
- [x] manual visual review of the fixed 1440×900 evidence confirms the Chronicle/Event Card/Focused Story composition stays compact and the map remains the primary surface.

### 5. Canonical World Stories gate — NEXT
- [x] deterministic seed45 browser path opens a meaningful causal Event Card through real player input;
- [x] player can inspect recorded causes/participants and follow at least one retained event ref + one map-capable entity ref;
- [x] player can focus an explicit stable world story, recover a coherent bounded trail, open a trail event, and clear focus;
- [x] player can preserve an event + entity across ordinary Chronicle browsing and same-tab reload through presentation-only Watchlist refs;
- [x] player can navigate retained history through compact Highlights/Recent/Conflict/Rule lenses without changing default representative history;
- [x] Event Card / Focused Story / Watchlist / Chronicle-lens navigation does not mutate authoritative snapshots/world fingerprint;
- [ ] one canonical first-time-player World Stories browser path proves the complete release promise in one coherent session;
- [ ] final merged-main CI + Pages + Chromium + package/tag/release `v0.5.0`.

## Stop rules

- No new god powers or v0.4 breadth.
- No ecology/species research.
- No economy/trade/religion/technology/civilization-depth expansion.
- No full replay/rewind engine.
- No graph database or generalized knowledge system.
- No AI-authored canonical facts, motives or causal links.
- Optional AI summary is not a blocker and is deferred unless deterministic story presentation proves insufficient.
- Presentation focus/bookmark/navigation state never enters authoritative world snapshots.
