# v0.8 backlog — Ruling Lines & Succession

Release gate: #255.

Version promise:

> **I can watch a ruling bloodline inherit power across generations, see when that bloodline loses the throne, and follow the new ruling line in the same causal world history.**

v0.8 is one finite Civilization Depth stage. It is **not** economy + religion + technology + naval warfare in one release.

## Semantic boundaries

- `parental_union` stays historical co-parent identity; it does not become marriage, spouse, household, exclusivity, inheritance or political alliance.
- existing maternal `lineage` stays a lineage primitive; it does not become a noble house/dynasty.
- **ruling line** is political identity derived from explicit parent→child descent.
- retained genealogy may resolve descendants after dead ancestors leave current humans; no second ancestry database may be introduced.
- succession selection consumes no RNG.

## Ordered capabilities

### 1. Genealogical succession resolver + trajectory audit — #257 / #258 — DONE + MERGED-MAIN DELIVERED

- [x] pure `engine/core/succession_genealogy.js` reconstructs explicit parent→child authority from current `parentIds` plus persistent parental-union child records;
- [x] cycle-safe descendant distance and deterministic ranking = distance asc → age desc → stable ID asc;
- [x] dead intermediate ancestry remains traversable without resurrecting humans;
- [x] paternal descent works independently from maternal `lineageId`;
- [x] co-parent identity does not imply spouse/household semantics;
- [x] resolver is mutation/RNG/snapshot neutral;
- [x] temporary seed45 shadow audit froze the first true selection divergence, then was deleted;
- [x] frozen transition: Eldergate Realm #1, day 9150 / Y25.4166667, previous ruler / shadow founder #23, baseline successor #28, only eligible descendant Human #31 (direct child, distance 1);
- [x] all 12 earlier successions after founding had no eligible descendant, so proposed and baseline ruler selection were identical before the transition;
- [x] #258 merged as `88ab84b626b30a1635e23c702b5dba30824d3bb7`;
- [x] merged-main CI #827, Pages #61/public `/play/`, full visual-qa #320 green.

### 2. Authoritative ruling-line succession — #259 / #260 — BRANCH GATE COMPLETE

#### Authority
- [x] first ruler appointment remains the existing oldest-eligible-adult founding choice and starts ruling line #1;
- [x] later succession ranks eligible descendants of `rulingLineFounderId` using the permanent genealogy resolver;
- [x] nearest generation → older age → lower stable ID;
- [x] when no eligible descendant exists, the exact existing oldest-adult fallback remains and starts the next ruling line;
- [x] vacancy clears only current ruler authority and preserves the ruling line for a later descendant fill;
- [x] no succession RNG;
- [x] polity owns only current political identity: `rulingLineFounderId`, `rulingLineSequence`, `rulingLineSinceDay`, `rulingLineReignCount`;
- [x] no House/Dynasty/Claim object or second genealogy authority introduced.

#### Recorded facts
- [x] ruler appointment/succession events preserve existing ruler IDs, `reason` and death/previous-ruler cause links;
- [x] events additionally record `successionPath`, current/previous ruling-line founder, line sequence, reign count, line-change boolean and descendant distance where applicable;
- [x] vacancy events retain current ruling-line identity without pretending the bloodline vanished.

#### Snapshot v16
- [x] snapshot schema advanced from v15 to v16 because polity now owns ruling-line state;
- [x] v15 and earlier accepted through explicit deterministic migration;
- [x] legacy anchor = current ruler, otherwise last ruler, otherwise null;
- [x] migrated anchor uses line #1 / reign count 1 / `rulingLineSinceDay = null`; no fabricated historical date/event;
- [x] current v16 snapshot round-trip and save/load continuation exact.

#### Canonical authority evidence
- [x] real seed45 implementation stays ruler/founder #23, line #13 immediately before day 9150;
- [x] day 9150 authoritative successor becomes **Human #31**, not baseline #28;
- [x] #31 is recorded as direct descendant (`distance=1`) and continues founder #23 / line #13;
- [x] duplicate and save/load continuation tests match exactly;
- [x] normal CI #828/#829/#830/#834/#835 green across authority and QA-only maintenance heads.

#### Prior-release regression compatibility
v0.7 tag/release evidence remains immutable: source `7f07ed67`, fork `67543ff4`.

v0.8 intentionally changes political history before the Y40 Scenario base, so current-main deterministic Scenario fingerprints advanced to:
- source `b411c106`;
- fork `0f28ca42`.

The full create/share/fresh-open/Replay/Fork exactness journey remains required. Independent portable and Replay/Fork Chromium gates repeatedly reproduced those values under v0.8.

The canonical Scenario QA helper was also hardened after repeated test-only failures:
- browser-level CDP target attachment no longer depends on Chrome implicitly creating a first page;
- failed profile cleanup cannot mask the original QA error;
- canonical Fork/Run assertions were restored to the already-shipped product contract (`FORK · EDITING · PAUSED`, `FORK · 3`, `#scenario-setup-run`).

Final implementation head `75e0a11929f9f4d1bc2dad7b530e339b9a63b5c6` passed CI #835 + full visual-qa #328. No client/UI/story semantics changed in this capability.

Capability-2 exit remaining:
- [ ] documentation-synchronized final head CI + full Visual QA green;
- [ ] squash merge #260;
- [ ] merged-main CI + Pages/public `/play/` + full Visual QA green.

### 3. Ruling-line readability — NEXT after Capability 2 merged-main delivery

- [ ] polity/ruler inspection shows current ruler, ruling-line founder, line sequence and reign count;
- [ ] dead/unavailable founder remains truthful stable identity;
- [ ] succession status distinguishes bloodline continuation vs new ruling line;
- [ ] no politics dashboard or family-tree editor.

### 4. Dynastic World Stories

- [ ] `polity.ruler_succeeded` presentation distinguishes recorded ruling-line continuation/change;
- [ ] Event Card retains real death/previous-ruler/successor/polity references;
- [ ] Rule lens keeps meaningful succession transitions visible;
- [ ] Focused Story remains on supported polity/human refs; lineage/union need not become new focus kinds;
- [ ] no AI-authored motive, legitimacy claim or inferred family politics.

### 5. Canonical Ruling Lines gate

One coherent headless + production Chromium path must prove:
- [ ] deterministic founding line;
- [ ] at least one real descendant-first succession;
- [ ] at least one truthful ruling-line change/fallback with no eligible descendant;
- [ ] exact causal history and readable Event Card/Rule-lens presentation;
- [ ] real map navigation to current polity/successor;
- [ ] save/load or duplicate-run exactness;
- [ ] paused story/inspection actions remain read-only;
- [ ] all v0.4–v0.7 browser regressions remain green.

A real existing God Power may trigger a canonical succession quickly, but no test-only heir/event/ruler mutation is allowed.

### 6. Release-only `v0.8.0` handoff

Only after capability 5 merges and merged-main delivery is green:
- [ ] package/docs release candidate only;
- [ ] annotated `v0.8.0` tag + GitHub Release;
- [ ] release-commit CI, Pages/public `/play/`, full Chromium;
- [ ] docs-only shipped-state closeout without moving the tag.

## Explicit non-goals

No economy/trade/market/currency/storage, professions/classes, marriage/spouse/household/marriage diplomacy, noble-house graph/titles/claims/legitimacy, elections/configurable succession-law UI, claimant civil-war framework, religion/culture/technology, boats/naval warfare, broad diplomacy rewrite, terrain/editor expansion, AI-authored political facts, fertility/migration/population rescue or controller guaranteeing dynasty survival.

## Product stop rule

A v0.8 slice enters only if it directly improves:

**ruler → explicit bloodline succession or line failure → readable political history.**
