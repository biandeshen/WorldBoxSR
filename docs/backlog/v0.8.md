# v0.8 backlog — Ruling Lines & Succession

Release gate: #255.

Version promise:

> **I can watch a ruling bloodline inherit power across generations, see when that bloodline loses the throne, and follow the new ruling line in the same causal world history.**

v0.8 is one finite Civilization Depth stage. It is **not** economy + religion + technology + naval warfare in one release.

## Semantic boundaries

- `parental_union` stays historical co-parent identity; it does not become marriage, spouse, household, exclusivity, inheritance or political alliance.
- existing maternal `lineage` stays a lineage primitive; it does not become a noble house/dynasty.
- **ruling line** is political identity derived from explicit parent→child descent.
- retained genealogy may resolve descendants after dead ancestors leave current humans; no second ancestry database may be introduced.

## Frozen intended minimal succession policy

Founding appointment remains the current deterministic oldest-eligible-adult selection.

For later succession:
1. identify eligible living polity adults who are descendants of the current ruling-line founder;
2. prefer nearest descendant generation;
3. within that generation preserve current age-descending, stable-ID ordering;
4. if no eligible descendant exists, use the current oldest-eligible polity-adult fallback;
5. that fallback starts a new ruling line;
6. vacancy remains truthful when no eligible adult exists;
7. succession selection consumes no RNG.

This is a small WorldBoxSR rule, **not** a historical primogeniture/elective/agnatic simulator.

## Ordered capabilities

### 1. Genealogical succession resolver + trajectory audit — #257 / #258 — BRANCH GATE EVIDENCE COMPLETE

#### Permanent resolver
- [x] `engine/core/succession_genealogy.js` reconstructs explicit parent→child adjacency from persistent parental-union child records plus current human `parentIds`;
- [x] duplicate evidence is deduplicated;
- [x] shortest descendant generation distance is cycle-safe;
- [x] dead/removed intermediate ancestors remain traversable from retained union records;
- [x] paternal descent works across maternal `lineageId` mismatch;
- [x] co-parenting/union membership does not imply spouse/descendant semantics;
- [x] ranking of an already-eligible candidate set = distance asc → age desc → stable human ID asc;
- [x] resolver is snapshot/RNG neutral and owns no persistence state;
- [x] permanent artificial-world tests lock all above contracts.

#### Temporary seed45 audit — frozen, then removed
The audit ran **current unmodified v0.7 succession** and maintained only a derived shadow ruling-line state. It distinguished:
- no-descendant fallback line changes;
- compatible descendant continuations where baseline already chose the top descendant;
- the first **true selection divergence** where descendant-first policy would select a different ruler.

CI #823 evidence, seed45 · 24×24 · population 30:
- requested horizon: 200 years;
- probe naturally stopped at **Y25.4166667 / day 9150**;
- ruler appointments before divergence: `1`;
- ruler successions before/including divergence: `13`;
- vacancies: `0`;
- no-descendant fallback line changes before divergence: `12`;
- compatible descendant continuations before divergence: `0`.

First frozen no-descendant line change:
- Y21.75 / day 7830;
- polity **#1 Eldergate Realm**;
- previous ruler / shadow line founder **#28**;
- baseline successor **#9**;
- succession event **#23**;
- 5 eligible adults;
- zero eligible descendants.

First frozen true selection divergence:
- **Y25.4166667 / day 9150**;
- polity **#1 Eldergate Realm**;
- previous ruler / shadow ruling-line founder **#23**;
- shadow line sequence `13` under the audit rule;
- baseline oldest-adult successor **#28**;
- succession event **#41**;
- 6 eligible adults;
- exactly one ranked eligible descendant: **Human #31**;
- Human #31 is a **direct child** (`distance=1`), age **24.0167y**, settlement **#1**;
- proposed descendant-first successor = **#31**, so this is the first authoritative ruler-selection divergence.

Because every earlier succession had no eligible descendant, current fallback and proposed policy choose the same successor before this point. The stop rule does **not** trigger: no fertility/migration/rescue/hidden heir/test-only succession is needed.

The temporary probe was deleted after evidence was frozen. Permanent CI keeps only resolver/tests.

Capability-1 exit remaining:
- [ ] final #258 head CI + full Visual QA green;
- [ ] squash merge #258;
- [ ] merged-main CI + Pages/public `/play/` + full Visual QA green.

No `rulers.js`, polity persistence, snapshot schema, presentation or succession-event behavior changes belong in capability 1.

### 2. Authoritative ruling-line succession — NEXT only after #258 merged-main delivery

- [ ] minimal polity political identity for current ruling line only; no duplicated genealogy;
- [ ] first appointment starts ruling line #1;
- [ ] eligible descendant succession continues the line;
- [ ] no eligible descendant uses current open-selection fallback and starts a new line;
- [ ] explicit succession facts record line continuation/change, founder IDs and descendant distance where applicable;
- [ ] death/no-longer-member/vacancy causes remain truthful;
- [ ] no sequential RNG draw;
- [ ] snapshot schema migration and exact save/load continuation;
- [ ] preserve current oldest-adult fallback when no eligible descendant exists;
- [ ] seed45 canonical implementation must first change successor at the frozen Y25.4167 transition from baseline #28 to direct child #31 unless an earlier new-rule line state is proven to alter the canonical path.

### 3. Ruling-line readability

- [ ] polity/ruler inspection shows current ruler, ruling-line founder, line sequence and reign count;
- [ ] dead/unavailable founder remains truthful stable identity;
- [ ] succession status distinguishes bloodline continuation vs new ruling line;
- [ ] no politics dashboard or family-tree editor.

### 4. Dynastic World Stories

- [ ] `polity.ruler_succeeded` presentation distinguishes recorded ruling-line continuation/change;
- [ ] Event Card retains real death/previous-ruler/successor/polity references;
- [ ] Rule lens keeps meaningful succession transitions visible;
- [ ] Focused Story remains on supported polity/human refs; lineage/union need not become new focus kinds;
- [ ] no AI-authored motive, legitimacy claim or inferred family politics.

### 5. Canonical Ruling Lines gate

One coherent headless + production Chromium path must prove:
- [ ] deterministic founding line;
- [ ] at least one real descendant-first succession;
- [ ] at least one truthful ruling-line change/fallback with no eligible descendant;
- [ ] exact causal history and readable Event Card/Rule-lens presentation;
- [ ] real map navigation to current polity/successor;
- [ ] save/load or duplicate-run exactness;
- [ ] paused story/inspection actions remain read-only;
- [ ] all v0.4–v0.7 browser regressions remain green.

A real existing God Power may trigger a canonical succession quickly, but no test-only heir/event/ruler mutation is allowed.

### 6. Release-only `v0.8.0` handoff

Only after capability 5 merges and merged-main delivery is green:
- [ ] package/docs release candidate only;
- [ ] annotated `v0.8.0` tag + GitHub Release;
- [ ] release-commit CI, Pages/public `/play/`, full Chromium;
- [ ] docs-only shipped-state closeout without moving the tag.

## Explicit non-goals

No economy/trade/market/currency/storage, professions/classes, marriage/spouse/household/marriage diplomacy, noble-house graph/titles/claims/legitimacy, elections/configurable succession-law UI, claimant civil-war framework, religion/culture/technology, boats/naval warfare, broad diplomacy rewrite, terrain/editor expansion, AI-authored political facts, fertility/migration/population rescue or controller guaranteeing dynasty survival.

## Product stop rule

A v0.8 slice enters only if it directly improves:

**ruler → explicit bloodline succession or line failure → readable political history.**
