# v0.6 backlog — Living Ecology

Release gate: #223. Version promise: **I can watch animal populations and vegetation affect each other, and see predation change that living system.**

v0.6 is a supported-scenario ecology stage, not a universal ecosystem simulator. Existing deterministic creature/resource authority is reused; no renderer or survival controller may become a second ecology model.

## Supported release scope

- canonical map size: **24×24**;
- existing/default sandbox behavior remains compatible unless the player explicitly chooses the supported Living Ecology preset;
- validated grazer natural-fauna seed: exactly 10 founders using the Sprint021 vegetation-rich top32 placement + keyed starting ages `[0,6y]`;
- grazer ecology settings for the preset: reproduction `0.001`, gradual old-age mortality enabled, existing hunger/grazing/starvation/resource movement unchanged;
- one additional named predator species only: **Wolf**;
- coherent extinction is allowed; the release does not guarantee long-term coexistence or equilibrium.

## Prior evidence we accept as binding

- Sprint021: exact 10-founder initializer passed 30/30 24×24 seeds for 120 years, with multi-generation reproduction, repeated vegetation pressure/recovery and unchanged sequential RNG;
- larger 32×32/48×48 research does not require founder up-scaling, but those sizes are not part of the v0.6 release promise;
- compact 16×16 initialization remains unresolved and is explicitly outside the canonical gate;
- scalar founder-count tuning is rejected as a universal compact fix;
- single terminal population thresholds are phase-sensitive and are not valid equilibrium gates;
- 40-year low-population recovery envelope failed unseen validation;
- encounter-safe founder placement passed an initial paired study but failed fresh validation (4 rescues vs 6 harms) and is rejected;
- default worlds were intentionally left creature-free by those research sprints, so v0.6 must productize a **supported preset**, not silently reinterpret old evidence as universal defaults.

## Ordered slices

### 1. Supported natural-fauna preset — NEXT
Player result: one explicit Living Ecology world starts with real natural grazers whose later births/deaths/resource pressure emerge from the existing authoritative system rather than manual post-warmup spawning.

- [ ] define one explicit preset identity in engine/presentation input; no broad Scenario system;
- [ ] preset is fixed to supported 24×24 scope for the v0.6 gate;
- [ ] implement the exact validated 10-founder deterministic grazer initializer, including keyed founder ages;
- [ ] enable only the validated grazer reproduction/old-age settings inside the preset;
- [ ] ordinary sandbox/default behavior remains compatibility-safe;
- [ ] initialization consumes no sequential world RNG and produces explicit normal creature authority/history only;
- [ ] headless deterministic test proves exact founder placement/age identity and repeatability;
- [ ] 24×24 bounded regression proves natural births and vegetation pressure without population targeting;
- [ ] real Chromium exposes the preset and visibly shows naturally seeded grazers without debug-only controls.

### 2. Multi-species creature surface
Player result: grazers and wolves are visually distinct, selectable and truthfully inspectable through one creature presentation path.

- [ ] extend creature projection so species is preserved rather than exposing a grazer-only view;
- [ ] preserve existing grazer behavior/appearance contracts while adding a clearly distinct Wolf silhouette/marker;
- [ ] selection/inspector identifies species, age, hunger and health for both species;
- [ ] no generic species plugin/rules DSL; refactor only what two shipped species require;
- [ ] existing creature refs/history resolution remain stable across the new species;
- [ ] deterministic presentation tests prove projection does not mutate world state/RNG;
- [ ] real screenshot review keeps animals readable but subordinate to the map.

### 3. Authoritative Wolf predation loop
Player result: wolves visibly seek and kill grazers because of real simulation needs; the resulting death is recorded causally and changes the living ecology.

Minimum allowed Wolf model:
- [ ] explicit authoritative `species: 'wolf'` creature state;
- [ ] hunger/health/age/lifecycle use the existing creature authority patterns where possible;
- [ ] deterministic prey-seeking movement toward living grazers within a bounded local search contract;
- [ ] adjacent/valid hunt kills one authoritative grazer through shared creature lifecycle bookkeeping;
- [ ] feeding reduces wolf hunger; failed hunting does not fabricate success;
- [ ] predation records explicit prey/predator refs and a truthful death cause/history event;
- [ ] no renderer-side combat and no hidden prey spawning;
- [ ] Wolf reproduction is included only if the supported canonical scenario cannot produce a coherent multi-generation predator loop without it; otherwise defer it rather than inventing a broad breeding framework;
- [ ] save/load continuation and snapshot migration preserve Wolf state exactly;
- [ ] keyed randomness/new ecology choices do not perturb unrelated sequential RNG;
- [ ] deterministic tests cover hunt/no-prey/starvation/death/save-load/repeatability.

### 4. Ecology readability pass
Player result: the ecology is understandable as play, not merely correct in the engine.

- [ ] compact species counts/state cues only where they help observation; no analytics dashboard;
- [ ] selected creature inspector makes hunger/health/species and relevant current behavior readable;
- [ ] vegetation pressure is visible through the existing terrain/vegetation surface or one restrained contextual cue, not a permanent heatmap requirement;
- [ ] predation produces a readable event/pulse/Chronicle projection from authoritative history;
- [ ] birth/death/predation wording never invents motives or unrecorded prey links;
- [ ] reduced-motion/accessible presentation remains respected;
- [ ] real Chromium demonstrates that a player can visually tell grazer activity from wolf hunting.

### 5. Canonical Living Ecology gate
One deterministic supported 24×24 world must prove the release promise in both headless and real-browser form.

Required facts:
- [ ] exact supported natural-fauna initialization occurs once with no hidden later rescue spawn;
- [ ] both Grazer and Wolf are visible and selectable;
- [ ] at least one natural grazer birth occurs;
- [ ] at least one authoritative Wolf→Grazer predation kill occurs;
- [ ] vegetation utilization changes materially under grazing pressure and later demonstrates recovery from a lower-pressure state within the supported gate window;
- [ ] the player can inspect the predator/prey consequence without raw engine JSON;
- [ ] extinction of either species is allowed if the sequence is causally coherent; no survival/equilibrium target is asserted;
- [ ] duplicate headless runs are byte-identical;
- [ ] save→load continuation reaches the same authoritative result as uninterrupted continuation;
- [ ] full Chromium gate exercises the actual preset/UI, captures fixed screenshots and proves no renderer-owned ecology state;
- [ ] performance stays credible for the supported entity counts/map size.

### 6. Release handoff
Only after slice 5 merges and merged-main CI + Chromium + Pages/public `/play/` succeed:

- [ ] bump package `0.5.0` → `0.6.0`;
- [ ] add release notes + canonical manual/browser QA;
- [ ] update README/ROADMAP/STATUS from target to shipped;
- [ ] verify tag `v0.6.0`, GitHub Release, merged-main CI, Chromium, Pages and public `/play/`;
- [ ] close #223 and freeze v0.6 feature breadth before v0.7 planning.

## Drift guards / non-goals

- No universal 16×16–48×48 natural-fauna initializer.
- No guaranteed ecology equilibrium, minimum species population or persistence controller.
- No hidden creature rescue/reseed after initialization.
- No disease, seasons, weather/climate model, genetics/evolution or plant-species simulation.
- No generalized N-species food-web graph/rules engine.
- No economy/trade/religion/technology/civilization-depth expansion.
- No new god-power breadth.
- No v0.5 Chronicle/AI/replay expansion unless a concrete ecology regression blocks the v0.6 gate.
- No more than three consecutive rejected ecology hypotheses that fail to unlock the visible gate; after that, narrow supported scope instead of extending research indefinitely.

## Product stop rule

Do not add another ecology mechanic merely because it is plausible. A slice enters v0.6 only if it directly improves the supported player's ability to answer:

**What species are here, what are they doing to vegetation and each other, and what happened after predation?**
