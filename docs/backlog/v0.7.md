# v0.7 backlog — Scenario Builder & Sharing

Release gate: #240. Version promise: **I can assemble a world setup, share/import its compact deterministic Scenario Recipe, reopen exactly the same starting world, then Run, Replay or Fork a different history.**

**Implementation is frozen at `1043a63375fee4ccaa72141da7f1e026a550b989`. Release-only handoff #252 / PR #253 is active.**

## Frozen release contract

A Scenario Recipe is deterministic startup input, never a live-world snapshot.

- kind/version: `worldboxsr-scenario` / `1`;
- base: current 24×24 / 40-year showcase-ready `sandbox | living_ecology` startup;
- setup: ordered Human / Grazer / Wolf placement only;
- max 32 actions, max 10 entities/action;
- name <=64 code units;
- canonical JSON <=8192 UTF-8 bytes;
- portable URL = unpadded base64url under exactly `scenario=`;
- invalid kind/version/preset/action/species/coordinates fail explicitly;
- Phaser is the Scenario product surface; Legacy remains comparison-only;
- no Recipe present remains compatible with ordinary startup.

## Ordered slices

### 1. Recipe v1 core — #242 / #243 — DONE + merged-main delivered
- [x] strict normalized schema + stable canonical serialization;
- [x] whole-recipe passability preflight before first command;
- [x] one materializer reusing production world creation + authoritative commands;
- [x] duplicate materializations byte-identical and metadata/codec RNG-neutral;
- [x] merge `910a98445428cd361025b028387be065d70034f1`;
- [x] CI #756, Pages #53/public `/play/`, visual #253 green.

### 2. Scenario Setup — #244 / #245 — DONE + merged-main delivered
- [x] visible paused `＋ Scenario` workflow;
- [x] existing Phaser pointer path remains the only map-input path;
- [x] successful placement appends only after authoritative command success;
- [x] failed placement is Recipe/world/identity neutral;
- [x] Clear rematerializes; Run freezes without changing the start;
- [x] canonical source start `7f07ed67`;
- [x] merge `8efcb523148a4728d422de7abaeff8527caea5df`;
- [x] CI #773, Pages #54/public `/play/`, visual #270 green.

### 3. Portable Recipe — #246 / #247 — DONE + merged-main delivered
- [x] canonical JSON export/import + unpadded-base64url `scenario=`;
- [x] Copy Link uses normalized Recipe only, not live authority;
- [x] fresh shared URL reconstructs exact paused start through the one materializer;
- [x] invalid visible Import is atomic and leaves URL/current Scenario unchanged;
- [x] canonical authored/shared/imported start = day 14400 / `7f07ed67`;
- [x] merge `27f8c1f8715d9908acb7bb95e264bfb5b3ad2233`;
- [x] CI #794, Pages #55/public `/play/`, visual #291 green.

### 4. Replay + Fork — #248 / #249 — DONE + merged-main delivered
- [x] Replay rematerializes frozen Recipe start; no timeline rewind/event replay/snapshot restore;
- [x] stale transient story/Inspector/selection state clears after world replacement while stable Watchlist refs can re-resolve;
- [x] Fork/Edit uses an independent normalized copy and retains immutable source identity separately;
- [x] fixed fourth Human `(12,8)` creates deterministic fork `67543ff4` from source `7f07ed67`;
- [x] dirty fork Replay returns exact fork while source canonical stays unchanged;
- [x] merge `9b44c03df6898096a422822e4af024cf744b9faa`;
- [x] CI #804, Pages #56/public `/play/`, visual #301 green.

### 5. Canonical Scenario Builder gate — #250 / #251 — DONE + implementation frozen

Canonical source **Portable trio**:
- seed45 Sandbox;
- Human `(12,8)` → Grazer `(16,12)` → Wolf `(14,7)`;
- paused day `14400`;
- source fingerprint **`7f07ed67`**.

Canonical fork:
- one fixed fourth Human `(12,8)`;
- fork fingerprint **`67543ff4`**.

- [x] dedicated headless gate proves duplicate source, exact transport round-trip, dirty→Replay exact source, Fork deterministic difference, duplicate fork and source replay after fork;
- [x] production Chromium starts from ordinary visible Setup and real map clicks;
- [x] Copy Link independently decodes to exact canonical Recipe;
- [x] fresh Chrome profile reaches byte-identical source `7f07ed67`;
- [x] Time/Play + real Meteor diverges source to `dfbad7ff`, Recipe unchanged;
- [x] Replay restores exact source and clears stale transient presentation;
- [x] Fork/Edit + fourth Human creates exact `67543ff4`, source canonical immutable;
- [x] ordinary fork gameplay diverges to `e0b5b305`;
- [x] Replay restores exact fork / `SCENARIO · 4`, source identity still unchanged;
- [x] canonical evidence records `canonicalJourneyComplete: true`;
- [x] implementation gate head `8aca62fcb4ae31b4ab3ab8bab9aa003cc750846d` passed CI #808 + visual #303;
- [x] artifact `9580488110` + four canonical 1440×900 screenshots passed manual review;
- [x] final docs head passed CI #810 + visual #305;
- [x] #251 merged as **implementation freeze** `1043a63375fee4ccaa72141da7f1e026a550b989`;
- [x] merged-main CI #811, Pages #57 including final public `/play/`, visual #306 green.

**v0.7 product/gate behavior is now frozen.**

### 6. Release-only v0.7.0 handoff — #252 / #253 — ACTIVE

Allowed release candidate changes only:
- [x] package version `0.6.0 → 0.7.0`;
- [x] add `docs/releases/v0.7.0.md`;
- [x] add `docs/demos/v0.7.0.md`;
- [x] synchronize README / ROADMAP / STATUS / this backlog as **candidate/pending**;
- [ ] changed-file audit = exactly those seven allowed package/docs files;
- [ ] final release PR head normal CI green;
- [ ] final release PR head full Chromium green;
- [ ] squash merge release PR; merge commit becomes intended release identity;
- [ ] release workflow creates annotated tag `v0.7.0`;
- [ ] tag ultimately targets exactly the release merge commit;
- [ ] GitHub Release `WorldBoxSR v0.7.0` created from checked-in release notes;
- [ ] release-commit main CI green;
- [ ] release-commit Pages deploy + final public `/play/` green;
- [ ] release-commit full Chromium green;
- [ ] docs-only post-release closeout marks shipped without moving tag;
- [ ] close #252 and #240 only after immutable publication evidence is complete.

## Release truth boundaries

- Engine remains sole world truth.
- Recipe is startup input only, never live-state mirror.
- Release PR may not change `client/**`, `engine/**`, `content/**`, `tests/**`, Scenario/Recipe tools, visual-gate behavior or workflows.
- Replay remains Recipe rematerialization, never timeline rewind.
- Fork remains an independent Recipe copy with immutable source identity.
- No new v0.7 feature may enter after implementation freeze.

## Explicit non-goals

No terrain/biome/water/resource painter, live snapshot save-game UI, Setup undo/remove stack, rules DSL/scripts/objectives/scoring, cloud/accounts/workshop backend, AI-generated canonical authority, custom map sizes, new simulation mechanics, generic God-Power-to-Recipe conversion, short-link/compression protocol or timeline rewind/event replay.

## Product stop rule

v0.7 is frozen around:

**compose setup → share/import → exact same start → Run → Replay or Fork.**
