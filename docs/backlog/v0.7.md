# v0.7 backlog — Scenario Builder & Sharing

Release gate: #240. Version promise: **I can assemble a world setup, share/import its compact deterministic Scenario Recipe, and reopen exactly the same starting world before running, replaying or forking a different history.**

v0.7 productizes existing deterministic startup + commands. It is **not** the full terrain/world editor.

## Binding architecture facts

- engine snapshots already provide exact internal save/load continuation, but the browser has no player-facing live-world save/import/export/share workflow;
- current product startup is deterministic 24×24, seed + `Sandbox | Living Ecology`, followed by the existing 40-year showcase warmup;
- authoritative creation commands already exist for Human, Grazer and Wolf;
- Scenario Recipe v1, visible Scenario Setup and portable Recipe transport are merged product capabilities;
- `scenario=` is the one portable Recipe transport query key; it is distinct from existing `renderer=` selection;
- there is no authoritative terrain/biome mutation command today, so tile painting remains deliberately deferred rather than implemented as presentation-owned state.

## Frozen recipe scope

A v0.7 Scenario Recipe is a **versioned startup input, not a world snapshot**.

```json
{
  "kind": "worldboxsr-scenario",
  "version": 1,
  "name": "My scenario",
  "base": { "seed": 45, "preset": "sandbox" },
  "setup": [
    { "type": "spawn_human", "x": 8, "y": 8, "count": 1 },
    { "type": "spawn_creature", "species": "grazer", "x": 9, "y": 8, "count": 1 }
  ]
}
```

Release scope may not widen silently.

### Base semantics
- exactly current `sandbox | living_ecology` presets;
- exactly current supported 24×24 product world;
- exactly current 40-year showcase-ready materialization before setup actions;
- no recipe present = byte-compatible v0.6 startup behavior;
- blank/day0/custom-size worlds are not required in v0.7.

### Setup actions
Exactly three placement identities: Human, Grazer, Wolf.

All placements go through existing authoritative command paths. Bounds:
- max 32 setup actions;
- max 10 entities per placement action;
- integer current-world coordinates + existing passability validation;
- name is bounded presentation metadata and never enters authoritative snapshots;
- malformed/unknown kind/version/preset/action/species fails explicitly;
- portable canonical JSON is capped at 8192 UTF-8 bytes and encoded as unpadded base64url for `scenario=`;
- setup order is authoritative recipe input and is preserved exactly.

## Ordered slices

### 1. Versioned Scenario Recipe contract + deterministic materializer — #242 / #243 — DONE
- [x] strict `worldboxsr-scenario` v1 normalization/validation;
- [x] stable canonical JSON preserving setup order;
- [x] name <=64, base exactly current presets/startup;
- [x] <=32 Human/Grazer/Wolf actions, <=10 entities/action;
- [x] materializer reuses `createShowcaseWorld` → `evolveShowcaseWorld` → `applyCommand`;
- [x] whole-recipe passability preflight before first command;
- [x] duplicate materializations byte-identical;
- [x] empty Sandbox/Living Ecology recipes byte-equal direct ready worlds;
- [x] name/codec world + RNG neutral, no DOM/storage/URL/Phaser dependency;
- [x] merged #243 as `910a98445428cd361025b028387be065d70034f1`;
- [x] merged-main CI #756, Pages #53/public `/play/`, visual-qa #253 green.

### 2. Scenario Setup workspace — #244 / #245 — DONE
- [x] explicit `＋ Scenario` entry in Phaser;
- [x] current Seed/Mode ready base rematerialized and paused;
- [x] exactly Human/Grazer/Wolf setup palette using existing Phaser pointer→tile→`scene.useTool` path;
- [x] success applies authoritative command then appends draft; failed impassable placement changes neither draft nor authority identity;
- [x] name + `N/32` + bounded recent summary;
- [x] Clear rematerializes; same setup reproduces same fingerprint;
- [x] Run freezes Recipe without changing start; later gameplay cannot rewrite it;
- [x] Legacy hides Setup; no second setup runtime;
- [x] canonical browser start `7f07ed67`, base `938b50ec`, post-Run one-day authority `bcc9eaf2`;
- [x] #245 merged as `8efcb523148a4728d422de7abaeff8527caea5df`;
- [x] merged-main CI #773, Pages #54/public `/play/`, visual-qa #270 green.

### 3. Portable recipe — Copy Link + Export/Import JSON — #246 / #247 — DONE
- [x] canonical JSON export uses only Recipe v1, never `snapshotWorld`;
- [x] import validates/materializes completely before replacing current authority;
- [x] canonical UTF-8 Recipe <=8192 bytes → unpadded base64url `scenario=`;
- [x] `scenario=` coexists with `renderer=`/unrelated query state; player share link targets Phaser;
- [x] Copy Link uses current normalized Recipe, never transient live world;
- [x] clipboard failure exposes generated URL for manual copy;
- [x] Export downloads byte-exact canonical Recipe JSON;
- [x] fresh shared URL materializes through existing Recipe materializer and starts paused;
- [x] Legacy rejects Scenario links truthfully;
- [x] visible Import leaves URL unchanged; invalid import is atomic;
- [x] codec/query helpers own no simulation/snapshot/storage/RNG state;
- [x] canonical `Portable trio` authored/shared/imported starts all day 14400 / fingerprint `7f07ed67` / exact normalized Recipe;
- [x] impassable `(18,12)` import rejects atomically;
- [x] implementation head `5d7b2402602532382456c68be2224e685114cba2` passed CI #791 + visual-qa #288 + manual review;
- [x] final docs head passed CI #793 + visual-qa #290;
- [x] #247 merged as `27f8c1f8715d9908acb7bb95e264bfb5b3ad2233`;
- [x] merged-main CI #794, Pages #55/public `/play/`, visual-qa #291 green.

### 4. Replay + Fork — #248 / #249 — DONE at branch gate

Player result: a shared scenario is reusable, not a one-shot import.

- [x] `Replay Scenario` rematerializes the current frozen Recipe through the existing materializer and returns paused at the exact Recipe start;
- [x] Replay is not timeline rewind/event replay/snapshot restore;
- [x] ordinary Time + destructive God Power can diverge authority while frozen Recipe remains byte-unchanged;
- [x] Replay clears stale Event Card, Focused Story, Inspector and selection presentation after world replacement;
- [x] Replay does not delete stable Watchlist/session refs; opening a story on rebuilt authority re-resolves them;
- [x] imported/shared frozen Scenario remains read-only until explicit Fork/Edit;
- [x] pure `forkScenarioSetup` returns an independent normalized copy, while original frozen/source Recipe stays immutable;
- [x] Fork rematerializes the exact source start and enters the existing paused Setup workspace;
- [x] Fork is visibly labeled `FORK · EDITING · PAUSED` and reuses the existing Human/Grazer/Wolf setup input path;
- [x] `forkSource` is retained separately from editable draft/current frozen fork for identity checks;
- [x] adding one supported placement changes fork Recipe/world without changing source canonical string;
- [x] Run freezes edited fork; replaying the frozen fork restores exact fork start;
- [x] headless deterministic gate proves source Replay exactness, duplicate replay equality, independent fork identity, one expected entity/event difference, duplicate fork equality and source replay after fork;
- [x] real Chromium starts from fresh shared `Portable trio` source `7f07ed67`, then Time + Meteor diverges to `0d1d99b2` with source Recipe unchanged;
- [x] real Replay restores exact source `7f07ed67`, clears stale story/selection presentation and preserves bookmarked event storage;
- [x] real Fork starts at exact source authority with copied 3-action Recipe, then one Human at `(12,8)` creates 4-action fork fingerprint `67543ff4` while source Recipe stays byte-identical;
- [x] dirty fork Replay restores exact `67543ff4` fork start while original source identity remains unchanged;
- [x] implementation head `bb5925b33f45e5b702cea9715dbc0b279a7ac316` passed CI #801 + full visual-qa #298;
- [x] artifact `9579531599` preserves 69 regression files, `scenario-replay-fork-evidence.json` and fixed Replay/Fork screenshots;
- [x] manual 1440×900 review confirms Replay identity/status is readable, Fork workspace is explicit/compact, fork replay shows `SCENARIO · 4`, and map remains primary;
- [x] no undo/remove-action stack, timeline rewind, terrain/resource editing, snapshot UI, share-protocol redesign, storage/cloud or new simulation behavior entered capability 4.

### 5. Canonical Scenario Builder gate — NEXT

One coherent headless + production Chromium path proves the release promise end-to-end.

Required canonical path:
- [x] create one normalized recipe over fixed seed/preset and Human + Grazer + Wolf setup;
- [x] duplicate headless materializations are byte-identical;
- [x] browser visible Setup can create the deterministic start through real clicks;
- [x] exact paused starting fingerprint + visible setup summary already locked by capability 2;
- [x] Copy Link / export JSON / fresh URL / visible JSON Import prove exact cross-profile start identity in capability 3;
- [x] ordinary Time/Play + destructive God Power can diverge authority without rewriting Recipe;
- [x] Replay returns exact original start fingerprint;
- [x] Fork adds one supported placement, yields the expected deterministic difference and preserves original source canonical string;
- [x] replaying an unchanged fork returns exact fork start;
- [ ] one dedicated **single coherent release gate** stitches compose → share/import → Run → Replay → Fork into one browser/session story rather than relying only on slice-specific gates;
- [ ] final screenshots make Setup / Shared / Replay / Fork states obvious while map stays primary;
- [ ] full v0.4/v0.5/v0.6 regressions remain green on final gate head;
- [ ] merged-main CI + Pages/public `/play/` + full Chromium pass before release handoff.

### 6. Release handoff
Only after slice 5 merges and merged-main delivery succeeds:
- [ ] bump package `0.6.0 → 0.7.0`;
- [ ] add release notes + canonical browser/manual QA;
- [ ] update README/ROADMAP/STATUS/backlog to shipped;
- [ ] verify annotated tag `v0.7.0`, GitHub Release, CI, Chromium, Pages/public `/play/`;
- [ ] close #240 and freeze v0.7 breadth before v0.8 planning.

## Truth boundaries
- Engine remains the only world truth.
- Recipe is deterministic input to world creation/commands, never a live-state mirror.
- Recipe metadata/transport never enters `snapshotWorld`, `world.history` or RNG.
- Successful setup placements are authoritative commands; failed actions are not recorded in recipe.
- Setup actions cannot continue after Run unless explicit Replay/Fork returns to a setup start.
- Replay means rematerialize Recipe start, never timeline rewind.
- Fork creates a new editable Recipe without mutating imported/shared source.
- Existing no-recipe v0.6 startup is a regression contract.
- Phaser is the v0.7 product surface; Legacy remains comparison-only.

## Non-goals / stop rules
- No terrain/elevation/moisture/biome/water painter or arbitrary tile/resource editor.
- No player-facing live snapshot save-game UI.
- No rules DSL/mod scripts/objectives/victory scoring.
- No cloud/accounts/workshop/discovery backend.
- No AI-generated scenario authority.
- No new simulation/ecology/civilization mechanics.
- No generic conversion of every God Power into setup actions.
- No custom map size.
- No timeline rewind/event replay engine or setup undo/remove stack.
- If a builder request needs a new authoritative terrain mutation model, defer it instead of putting mutation in UI code.

## Product stop rule
A slice enters v0.7 only if it directly improves:

**compose setup → share/import → exact same start → Run → Replay or Fork.**
