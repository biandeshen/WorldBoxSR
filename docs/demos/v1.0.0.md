# v1.0.0 canonical demo — Stable Sandbox Identity

This is the release QA path for the v1.0.0 stable-sandbox contract.

Primary promise:

> **WorldBoxSR behaves like one dependable sandbox product: reproducible worlds, stable browser play, recoverable saves, understandable history, supported public surfaces, and coherent creation/intervention/civilization/ecology/story workflows.**

## Open the product

Public demo: `https://biandeshen.github.io/WorldBoxSR/play/`

Certified runtime/support statement: `docs/SUPPORT.md`.

Canonical desktop release surface:
- production Phaser;
- Chrome/Chromium class;
- 1440×900;
- seed45 ordinary showcase path;
- public 24×24 world surface.

Canonical touch release surface:
- production Phaser;
- Chrome/Chromium class coarse-touch emulation;
- 430×820.

## Gate 1 — portable compatibility

The release test suite must keep:
- current `SNAPSHOT_VERSION = 16`;
- `SUPPORTED_SNAPSHOT_VERSIONS = [10,11,12,13,14,15,16]`;
- local ordinary-world envelope v1;
- Scenario Recipe v1.

Independent compatibility cases must exist for all seven engine versions and each must:
1. restore successfully;
2. normalize to current v16;
3. leave the input snapshot unchanged;
4. restore through local envelope v1;
5. remain exact against a fresh restore of the normalized v16 state after the same 12-tick continuation.

Required explicit failures:
- below-floor/unsupported/future engine version;
- future local envelope version;
- unsupported embedded engine snapshot;
- future Scenario Recipe version.

Historical provenance:
- v15 is the public snapshot format used by v0.3.0/v0.4.0/v0.7.0;
- v16 is the public snapshot format used by v0.8.0/v0.9.0/current;
- v10–v14 are older accepted prototype/internal schemas, not public release fixtures.

## Gate 2 — certified desktop product

At 1440×900 production Phaser:
- warmup must reach `Phaser 4 · authoritative simulation · showcase ready`;
- `Renderer failed:` must be absent;
- canonical world remains the primary visual surface;
- real God Power pointer interaction still works;
- causal World Stories navigation still works;
- Scenario Setup real pointer/editor interaction still works;
- all historical v0.4–v0.9 full-scope browser regressions remain green.

Legacy comparison screenshot remains required, but Legacy is only an ordinary-world compatibility fallback, not feature-equivalent Phaser.

## Gate 3 — certified touch product

At 430×820 production Phaser, real CDP touch evidence must keep:
- core Pause / View / World controls reachable;
- no document-level horizontal overflow;
- short tap uses the selected tool and adds exactly one authoritative command;
- long hold inspects and consumes no additional command;
- drag semantics remain pan;
- two-finger pinch changes zoom without mutating world authority;
- finger midpoint world focus remains stable within the frozen tolerance;
- after both fingers release, a fresh one-finger action works normally.

Current certified touch contract:
- Tap: tool;
- Hold: inspect;
- Drag: pan;
- Pinch: zoom.

## Gate 4 — explicit renderer recovery

Fault-injected Chromium blocks the built `phaser_main` app chunk.

The failed product must expose:
- `Retry Phaser`;
- `Compatibility renderer`.

Compatibility action must open the existing Legacy renderer and preserve unrelated URL parameters. Scenario fallback consequences must remain disclosed; the release must not claim Legacy Scenario equivalence.

## Gate 5 — ordinary stable-sandbox composition

Use the existing production Session UI only.

Frozen v1.0 ordinary persistence path:
1. reach ordinary seed45 showcase ready;
2. Pause;
3. open Session;
4. **Save now** must be usable immediately once the world is ready — it must not wait for the 30-second autosave;
5. save the ordinary local envelope v1/current engine snapshot;
6. set Time to 1 day and resume until authority changes;
7. Pause;
8. Restore through the real UI/runtime;
9. require exact saved authoritative `JSON.stringify(world)` returned;
10. require saved day returned and Restore installed paused;
11. enter real Scenario Setup;
12. require ordinary Save/Restore disabled;
13. require exact status `Scenario active · use Recipe / Replay / Fork`;
14. require the previously created ordinary local save string remains unchanged.

Frozen implementation evidence:
- saved day `14400`;
- mutated day `14401`;
- restored day `14400`;
- saved/restored fingerprint `22296cba`;
- mutated fingerprint `be4daf1e`;
- exact Restore true;
- paused after Restore true;
- Scenario Save/Restore disabled;
- local ordinary save unchanged.

## Gate 6 — causal history composition

The existing World Stories evidence remains the authority owner for causal inspection.

Frozen implementation evidence includes:
- Lightning Event #175;
- death Event #176;
- succession Event #181;
- causal Event Card/cause navigation;
- map/Inspector navigation;
- read-only story navigation leaves authority unchanged.

## Gate 7 — canonical Scenario identity

The existing canonical Scenario Builder evidence remains the authority owner for Scenario exactness.

It must continue to prove:
- canonical Scenario Recipe v1 compose/share;
- exact fresh open;
- Run/diverge;
- exact Replay source;
- Fork/edit;
- Run/diverge;
- exact Replay fork.

Frozen implementation evidence:
- Recipe version `1`;
- source fingerprint `b411c106`;
- fork fingerprint `0f28ca42`;
- source and fork distinct;
- canonical journey complete true.

Ordinary local-world persistence must never replace active/shared Scenario identity.

## Gate 8 — final composed v1.0 evidence

In full visual scope, `verify-stable-sandbox-evidence.mjs` reads:
- `story-evidence.json`;
- `stable-sandbox-persistence-evidence.json`;
- `canonical-scenario-builder-evidence.json`.

It must write:
- `stable-sandbox-evidence.json`;
- `stableSandboxGateComplete: true`.

The implementation-freeze artifact at `efecf1e91ac88177ce82917c1312577927e26af6` records exactly that result.

## Gate 9 — structural build envelope

`npm run pages:check` must require:
- exactly one dedicated Phaser vendor chunk;
- exactly one Phaser app chunk;
- app chunk ≤300,000 bytes minified;
- vendor chunk larger than app chunk;
- `/play/` remains a runnable game artifact.

The 64×64 1k/10k Node benchmark is diagnostic only and must not become an uncalibrated release timing SLA.

## Explicit support boundary

Certified:
- Chrome/Chromium-class 1440×900 desktop Phaser;
- Chrome/Chromium-class 430×820 coarse-touch Phaser;
- Legacy ordinary-world compatibility fallback.

Uncertified/best effort:
- Firefox/Gecko;
- Safari/WebKit;
- arbitrary other browser/device/viewport combinations.

No cloud save or cross-device sync.

## Frozen implementation evidence

Implementation freeze:
`efecf1e91ac88177ce82917c1312577927e26af6`

Green exact-freeze delivery:
- CI #946;
- Pages #95/public `/play/`;
- full historical visual-qa #434;
- `stable-sandbox-evidence.json` present with `stableSandboxGateComplete: true`.

## Release-candidate verification

Before tagging:

```bash
npm test
npm run smoke
npm run pages:build
npm run pages:check
bash tools/capture-visual-evidence.sh
bash tools/run-touch-inspect-smoke.sh
bash tools/run-renderer-recovery-smoke.sh
VISUAL_QA_SCOPE=full bash tools/run-stable-sandbox-smoke.sh
```

After the release PR merges, require:
- existing release workflow succeeds;
- annotated tag `v1.0.0` targets exactly the release merge commit;
- GitHub Release `WorldBoxSR v1.0.0` exists from checked-in notes;
- release-commit CI succeeds;
- release-commit Pages/public `/play/` succeeds;
- release-commit full historical visual-qa succeeds, including the stable-sandbox composition artifact;
- the tag never moves during later docs-only closeout.
