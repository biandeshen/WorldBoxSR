import Phaser from 'phaser';
import { EntityLayer } from './presentation/entity_layer.js';
import { playToolEffect } from './presentation/effects_layer.js';
import { SettlementLayer } from './presentation/settlement_layer.js';
import { createTerrainLayer, renderTerrain } from './presentation/terrain_layer.js';
import { WarbandLayer } from './presentation/warband_layer.js';
import { advanceWorld, applyGodTool, createShowcaseWorld, evolveShowcaseWorld, selectionAt, SHOWCASE, worldSummary, worldView } from './presentation/world_adapter.js';
import { civilizationChronicle, formatChronicleDetail, formatChronicleLabel, latestCivilizationPulse, latestHistoryEventId } from './presentation/civilization_story.js';

const TILE_SIZE = 28;
const STEP_INTERVAL_MS = 110;
const seedInput = document.querySelector('#seed'); const resetButton = document.querySelector('#reset'); const resetCameraButton = document.querySelector('#reset-camera'); const pauseButton = document.querySelector('#pause'); const speedSelect = document.querySelector('#speed'); const toolSelect = document.querySelector('#tool'); const stats = document.querySelector('#stats'); const inspector = document.querySelector('#inspector'); const historyScopeLabel = document.querySelector('#history-scope-label'); const historyList = document.querySelector('#history-list'); const historyDetail = document.querySelector('#history-detail'); const bootStatus = document.querySelector('#boot-status'); const eventToast = document.querySelector('#event-toast');
if (bootStatus) bootStatus.textContent = 'Phaser 4 · module loaded · starting scene…';

class WorldScene extends Phaser.Scene {
  constructor() { super('world'); this.world = null; this.view = null; this.terrain = null; this.entities = null; this.settlements = null; this.warbands = null; this.paused = false; this.booting = false; this.worldGeneration = 0; this.nextStepAt = 0; this.lastHudAt = 0; this.drag = null; this.storyCursorId = 0; }
  create() { try { if (bootStatus) bootStatus.textContent = 'Phaser 4 · scene created · preparing world…'; this.entities = new EntityLayer(this, TILE_SIZE); this.settlements = new SettlementLayer(this, TILE_SIZE); this.warbands = new WarbandLayer(this, TILE_SIZE); this.bindInput(); this.bindDom(); this.game.canvas.addEventListener('contextmenu', (event) => event.preventDefault()); this.resetWorld(); } catch (error) { this.reportRendererFailure(error); throw error; } }
  update(time) { if (!this.world || !this.entities || !this.warbands) return; if (!this.booting && !this.paused && time >= this.nextStepAt) { const days = Math.max(1, Number(speedSelect?.value || 10)); advanceWorld(this.world, days); this.syncWorld(time); this.nextStepAt = time + STEP_INTERVAL_MS; } this.entities.update(time); this.warbands.update(time); if (time - this.lastHudAt > 220) { this.refreshHud(); this.lastHudAt = time; } }
  resetWorld() { const token = ++this.worldGeneration; const seed = seedInput?.value?.trim() || String(SHOWCASE.defaultSeed); if (seedInput && !seedInput.value.trim()) seedInput.value = String(SHOWCASE.defaultSeed); this.booting = true; this.world = createShowcaseWorld(seed); this.storyCursorId = latestHistoryEventId(this.world); this.view = worldView(this.world); if (this.terrain) this.terrain.destroy(); this.settlements.destroy(); this.entities.destroy(); this.warbands.destroy(); this.terrain = createTerrainLayer(this, this.view, TILE_SIZE); this.settlements.sync(this.view); this.entities.sync(this.view, this.time.now, 0); this.warbands.sync(this.view, this.time.now, 0); this.resetCamera(); this.refreshHud(); this.refreshChronicle(); if (bootStatus) bootStatus.textContent = `Phaser 4 · authoritative simulation · evolving showcase 0/${SHOWCASE.warmupYears}y`; this.showToast(`World ${seed} · evolving toward showcase`); window.setTimeout(() => { void this.finishShowcaseWarmup(token, seed); }, 0); }
  async finishShowcaseWarmup(token, seed) { try { await evolveShowcaseWorld(this.world, { onProgress: ({ year, targetYear }) => { if (token !== this.worldGeneration) return; if (bootStatus) bootStatus.textContent = `Phaser 4 · authoritative simulation · evolving showcase ${year.toFixed(0)}/${targetYear}y`; if (Math.round(year) % 4 === 0 || year >= targetYear) this.syncWorld(this.time.now); } }); if (token !== this.worldGeneration) return; this.syncWorld(this.time.now); this.storyCursorId = latestHistoryEventId(this.world); this.booting = false; this.nextStepAt = this.time.now + STEP_INTERVAL_MS; if (bootStatus) bootStatus.textContent = 'Phaser 4 · authoritative simulation · showcase ready'; this.showToast(`World ${seed} · showcase ready at year ${SHOWCASE.warmupYears}`); } catch (error) { if (token !== this.worldGeneration) return; this.booting = false; this.reportRendererFailure(error); console.error(error); } }
  syncWorld(now) { if (!this.world || !this.terrain) return; this.view = worldView(this.world); renderTerrain(this.terrain, this.view, TILE_SIZE); this.settlements.sync(this.view); this.entities.sync(this.view, now, Math.min(180, STEP_INTERVAL_MS * 1.3)); this.warbands.sync(this.view, now, Math.min(220, STEP_INTERVAL_MS * 1.7)); this.refreshChronicle(); this.refreshCivilizationPulse(); }
  resetCamera() { if (!this.view) return; const worldWidth = this.view.width * TILE_SIZE; const worldHeight = this.view.height * TILE_SIZE; const camera = this.cameras.main; camera.setBounds(0, 0, worldWidth, worldHeight); const fitX = Math.max(0.55, (this.scale.width - 40) / worldWidth); const fitY = Math.max(0.55, (this.scale.height - 130) / worldHeight); camera.setZoom(Math.min(1.45, fitX, fitY)); camera.centerOn(worldWidth / 2, worldHeight / 2); }
  bindInput() { this.input.on('pointerdown', (pointer) => { if (!pointer.leftButtonDown()) return; this.drag = { startX: pointer.x, startY: pointer.y, lastX: pointer.x, lastY: pointer.y, moved: false }; }); this.input.on('pointermove', (pointer) => { if (!this.drag || !pointer.isDown) return; const dx = pointer.x - this.drag.lastX; const dy = pointer.y - this.drag.lastY; const total = Math.hypot(pointer.x - this.drag.startX, pointer.y - this.drag.startY); if (total > 5) this.drag.moved = true; if (this.drag.moved) { const camera = this.cameras.main; camera.scrollX -= dx / camera.zoom; camera.scrollY -= dy / camera.zoom; } this.drag.lastX = pointer.x; this.drag.lastY = pointer.y; }); this.input.on('pointerup', (pointer) => { const drag = this.drag; this.drag = null; if (!drag || drag.moved || !this.world) return; const tile = this.pointerTile(pointer); if (!tile) return; if (pointer.event?.altKey || pointer.rightButtonReleased()) { this.inspectTile(tile.x, tile.y); return; } if (this.booting) { this.showToast('World is still evolving into the showcase…'); return; } const count = pointer.event?.shiftKey ? 10 : 1; this.useTool(tile.x, tile.y, count); }); this.input.on('wheel', (pointer, gameObjects, deltaX, deltaY) => { const camera = this.cameras.main; const factor = Math.exp(-deltaY * 0.0013); camera.setZoom(Phaser.Math.Clamp(camera.zoom * factor, 0.55, 2.6)); }); }
  bindDom() { resetButton?.addEventListener('click', () => this.resetWorld()); resetCameraButton?.addEventListener('click', () => this.resetCamera()); pauseButton?.addEventListener('click', () => { this.paused = !this.paused; pauseButton.textContent = this.paused ? '▶ Play' : 'Ⅱ Pause'; pauseButton.dataset.active = this.paused ? 'true' : 'false'; }); }
  pointerTile(pointer) { if (!this.view) return null; const point = this.cameras.main.getWorldPoint(pointer.x, pointer.y); const x = Math.floor(point.x / TILE_SIZE); const y = Math.floor(point.y / TILE_SIZE); if (x < 0 || y < 0 || x >= this.view.width || y >= this.view.height) return null; return { x, y }; }
  useTool(x, y, count) { try { const result = applyGodTool(this.world, toolSelect?.value || 'spawn_human', x, y, count); playToolEffect(this, result.effect, x, y, TILE_SIZE); this.syncWorld(this.time.now); this.inspectTile(x, y); this.showToast(toolMessage(result, count)); } catch (error) { if (/impassable/i.test(String(error?.message))) { this.showToast('That power needs passable land'); return; } console.error(error); this.showToast(`Power failed: ${error?.message || error}`); } }
  inspectTile(x, y) { const selection = selectionAt(this.world, x, y); if (!selection || !inspector) return; const target = selection.value; if (selection.kind === 'warband') { const polity = this.world.polities.find((candidate) => candidate.id === target.polityId); const opponent = this.world.polities.find((candidate) => candidate.id === target.opponentPolityId); const targetSettlement = this.world.settlements.find((candidate) => candidate.id === target.targetSettlementId); const opposingBand = (this.world.warbands ?? []).find((candidate) => candidate.active && candidate.relationKey === target.relationKey && candidate.warStartedDay === target.warStartedDay && candidate.polityId === target.opponentPolityId); const engaged = Boolean(opposingBand && Math.abs(target.x - opposingBand.x) + Math.abs(target.y - opposingBand.y) <= 1); inspector.textContent = [`⚑ Warband #${target.id}`, `${polity?.name ?? `Polity #${target.polityId}`} vs ${opponent?.name ?? `Polity #${target.opponentPolityId}`}`, `${engaged ? '⚔ engaged' : 'marching'} · strength ${target.strength}/${target.initialStrength}`, `engagements ${target.engagements} · captures ${target.captures ?? 0} · mobilized from ${target.sourceAdultPopulation} adults`, targetSettlement ? `objective: ${targetSettlement.name}` : `objective settlement #${target.targetSettlementId}`, `tile ${target.x},${target.y}`].join('\n'); return; } if (selection.kind === 'human') { const settlement = Number.isInteger(target.settlementId) ? this.world.settlements.find((candidate) => candidate.id === target.settlementId) : null; const polity = Number.isInteger(settlement?.polityId) ? this.world.polities.find((candidate) => candidate.id === settlement.polityId) : null; inspector.textContent = [`Human #${target.id}`, `${target.sex === 'F' ? 'female' : 'male'} · age ${(target.ageDays / this.world.config.daysPerYear).toFixed(1)}y`, polity?.rulerId === target.id ? `♛ ruler of ${polity.name}` : (polity ? `citizen of ${polity.name}` : 'no polity'), `health ${(target.health * 100).toFixed(0)}% · hunger ${(target.hunger * 100).toFixed(0)}%`, `tile ${target.x},${target.y} · settlement ${target.settlementId ?? 'none'}`].join('\n'); return; } if (selection.kind === 'creature') { inspector.textContent = [`Grazer #${target.id}`, `age ${(target.ageDays / this.world.config.daysPerYear).toFixed(1)}y`, `health ${(target.health * 100).toFixed(0)}% · hunger ${(target.hunger * 100).toFixed(0)}%`, `tile ${target.x},${target.y}`].join('\n'); return; } if (selection.kind === 'settlement') { const polity = Number.isInteger(target.polityId) ? this.world.polities.find((candidate) => candidate.id === target.polityId) : null; const previousPolity = Number.isInteger(target.previousPolityId) ? this.world.polities.find((candidate) => candidate.id === target.previousPolityId) : null; const rebelledFrom = Number.isInteger(target.lastRebelledFromPolityId) ? this.world.polities.find((candidate) => candidate.id === target.lastRebelledFromPolityId) : null; const relation = polity ? relevantRelation(this.world, polity.id) : null; const counterpartId = relation ? (relation.polityAId === polity.id ? relation.polityBId : relation.polityAId) : null; const counterpart = Number.isInteger(counterpartId) ? this.world.polities.find((candidate) => candidate.id === counterpartId) : null; const conquestYear = Number.isInteger(target.lastConqueredDay) ? (target.lastConqueredDay / this.world.config.daysPerYear).toFixed(1) : null; const rebellionYear = Number.isInteger(target.lastRebelledDay) ? (target.lastRebelledDay / this.world.config.daysPerYear).toFixed(1) : null; const rebellionEligibleYear = Number.isInteger(target.rebellionEligibleDay) ? (target.rebellionEligibleDay / this.world.config.daysPerYear).toFixed(1) : null; inspector.textContent = [target.name, `${target.active ? 'active settlement' : 'abandoned settlement'} · population ${target.population}`, polity ? `♛ ${polity.name}${polity.capitalSettlementId === target.id ? ' · capital' : ''}` : 'no polity', polity ? `♔ ruler ${Number.isInteger(polity.rulerId) ? `Human #${polity.rulerId}` : 'vacant'}` : '', conquestYear ? `⚑ conquered Y${conquestYear} from ${previousPolity?.name ?? `Polity #${target.previousPolityId}`} · total ${target.conquestCount ?? 0}` : '', rebellionEligibleYear && (target.rebellionCount ?? 0) < 1 ? `occupation · rebellion seed eligible Y${rebellionEligibleYear}` : '', rebellionYear ? `✦ rebelled Y${rebellionYear} from ${rebelledFrom?.name ?? `Polity #${target.lastRebelledFromPolityId}`}` : '', relation ? `${relation.atWar ? '⚔ WAR' : relation.stance.toUpperCase()} with ${counterpart?.name ?? `Polity #${counterpartId}`} · score ${relation.score}` : (polity ? 'diplomacy: no active counterpart' : ''), relation?.cause ? `pressure: ${relation.cause}` : '', `founded year ${(target.foundedDay / this.world.config.daysPerYear).toFixed(1)}`, `center ${target.x},${target.y}`].filter(Boolean).join('\n'); return; } inspector.textContent = [`Tile ${target.x},${target.y}`, `${target.biome} · ${target.passable ? 'passable' : 'impassable'}`, `elevation ${target.elevation.toFixed(2)} · moisture ${target.moisture.toFixed(2)}`, `vegetation ${target.vegetation.toFixed(2)} / ${target.vegetationCapacity.toFixed(2)}`].join('\n'); }
  refreshHud() { if (!stats || !this.world) return; const summary = worldSummary(this.world); const activePolities = this.world.polities.filter((polity) => polity.active).length; const activeWars = (this.world.relations ?? []).filter((relation) => relation.active && relation.atWar).length; const activeWarbands = (this.world.warbands ?? []).filter((warband) => warband.active).length; stats.innerHTML = [`<span><b>Year ${summary.year.toFixed(1)}</b></span>`, `<span>👤 ${summary.population}</span>`, `<span>🐾 ${summary.grazers}</span>`, `<span>♛ ${activePolities}</span>`, `<span>⚔ ${activeWars}</span>`, `<span>⚑ ${activeWarbands}</span>`, `<span>⌂ ${summary.activeSettlements}</span>`].join(''); }
  refreshChronicle() { if (!historyList || !historyScopeLabel || !this.world) return; historyScopeLabel.textContent = 'World chronicle · authoritative history'; const rows = civilizationChronicle(this.world, { limit: 7 }); historyList.innerHTML = rows.length ? rows.map((entry) => `<button class="history-event" type="button" data-event-id="${entry.eventId}">${escapeHtml(formatChronicleLabel(entry))}</button>`).join('') : '<div id="history-empty">No recorded events yet</div>'; historyList.querySelectorAll('button[data-event-id]').forEach((button) => { button.addEventListener('click', () => { const entry = rows.find((candidate) => String(candidate.eventId) === button.dataset.eventId); if (historyDetail) historyDetail.textContent = entry ? formatChronicleDetail(entry) : 'Event unavailable'; }); }); }
  refreshCivilizationPulse() { if (!this.world) return; const latestId = latestHistoryEventId(this.world); if (this.booting) { this.storyCursorId = latestId; return; } const pulse = latestCivilizationPulse(this.world, { afterEventId: this.storyCursorId }); this.storyCursorId = latestId; if (pulse) this.showToast(`${pulse.icon} ${pulse.headline}`); }
  showToast(message) { if (!eventToast) return; eventToast.textContent = message; eventToast.dataset.visible = 'true'; window.clearTimeout(this.toastTimer); this.toastTimer = window.setTimeout(() => { eventToast.dataset.visible = 'false'; }, 1500); }
  reportRendererFailure(error) { const message = error?.stack || error?.message || String(error); if (bootStatus) bootStatus.textContent = `Renderer failed: ${message}`; this.showToast(`Renderer failed: ${error?.message || error}`); }
}
let game; try { game = new Phaser.Game({ type: Phaser.AUTO, parent: 'phaser-root', backgroundColor: '#0a1118', pixelArt: true, roundPixels: true, render: { antialias: false, roundPixels: true }, scale: { mode: Phaser.Scale.RESIZE, width: window.innerWidth, height: window.innerHeight }, scene: [WorldScene] }); globalThis.__PHASER_GAME__ = game; } catch (error) { if (bootStatus) bootStatus.textContent = `Renderer failed: ${error?.stack || error?.message || error}`; console.error(error); throw error; }
function relevantRelation(world, polityId) { return (world.relations ?? []).filter((relation) => relation.active && (relation.polityAId === polityId || relation.polityBId === polityId)).sort((a, b) => Number(b.atWar) - Number(a.atWar) || a.score - b.score || a.key.localeCompare(b.key))[0] ?? null; }
function toolMessage(result, count) { if (result.effect === 'meteor') { if (result.noEffect) return '☄ Meteor struck · no living targets or vegetation'; const lives = (result.humanIds?.length ?? 0) + (result.creatureIds?.length ?? 0); return `☄ Meteor impact · ${lives} ${lives === 1 ? 'life' : 'lives'} · ${result.vegetationRemoved.toFixed(1)} vegetation`; } if (result.effect === 'lightning') return '⚡ Lightning struck'; if (result.effect === 'erase') return '✕ Erase applied'; if (result.effect === 'spawn_grazer') return `🐾 Spawned ${count} grazer${count === 1 ? '' : 's'}`; return `✦ Spawned ${count} human${count === 1 ? '' : 's'}`; }
function escapeHtml(value) { return String(value).replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#39;'); }
